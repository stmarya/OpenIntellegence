API_BASE_URL=http://localhost:8000/api/v1
# Server-only API key used by SSR and the allowlisted write proxy.
API_SERVICE_KEY=
# Separate operator token required for every write action. This is not an API key.
CONSOLE_ACTION_TOKEN=
NEXT_PUBLIC_API_BASE_URL=
