import { UnavailableState } from '@/components/UnavailableState';
export default function AlertsPage() { return <UnavailableState title="Alerts" detail="No tenant alert stream is bundled. Unknown alert state is not represented as clean or resolved." />; }
