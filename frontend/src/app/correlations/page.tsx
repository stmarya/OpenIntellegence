import { UnavailableState } from '@/components/UnavailableState';
export default function CorrelationsPage() { return <UnavailableState title="Correlations" detail="Correlation evidence is tenant-scoped and must arrive through the API with provenance and confidence." />; }
