import { UnavailableState } from '@/components/UnavailableState';
export default function AutomationPage() { return <UnavailableState title="Automation & playbooks" detail="Playbook runs and approvals require tenant API records. This UI does not dispatch actions or expose connector credentials." />; }
