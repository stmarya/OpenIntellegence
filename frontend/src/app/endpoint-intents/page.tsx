export default function EndpointIntentsPage() { return <main className="content"><p className="banner">Endpoint intents are approval-only control-plane requests. Delivery is always null until an approved backend capability exists.</p><h1>Endpoint intents</h1><section className="reference"><strong>No tenant intents available</strong><p>Supported intent types: isolate network, collect inventory, rotate agent certificate.</p><p>Required policy: requester exclusion, two distinct approvers, expiry, cancellation, and <b>pending / not dispatched</b> state.</p><small>No shell command, execution, completion, or delivery control exists in this UI.</small></section></main>; }
