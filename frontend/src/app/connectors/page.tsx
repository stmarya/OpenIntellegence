import { UnavailableState } from '@/components/UnavailableState';
export default function ConnectorsPage() { return <UnavailableState title="Connectors & health" detail="Connector health is configuration-aware and never exposes credentials, webhook URLs, or performs a browser-side probe." />; }
