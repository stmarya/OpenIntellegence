import { UnavailableState } from '@/components/UnavailableState';
export default function AssetsPage() { return <UnavailableState title="Assets" detail="Asset inventory requires authenticated tenant API data. Bundled threat-intelligence snapshots do not represent managed devices." />; }
